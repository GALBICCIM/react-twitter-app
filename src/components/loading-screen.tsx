import * as S from "../styles/LoadingStyle";

const LoadingScreen = () => {
   return (
      <S.Wrapper>
         <S.Text>Loading...</S.Text>
      </S.Wrapper>
   );
};

export default LoadingScreen;
